namespace Feliz.Plotly

(*////////////////////////////////
/// THIS FILE IS AUTO-GENERATED //
////////////////////////////////*)

open Fable.Core
open Fable.Core.JsInterop
open Feliz

[<Erase>]
type lightposition =
    /// Numeric vector, representing the X coordinate for each vertex.
    static member inline x (value: int) = Interop.mkLightpositionAttr "x" value
    /// Numeric vector, representing the X coordinate for each vertex.
    static member inline x (value: float) = Interop.mkLightpositionAttr "x" value
    /// Numeric vector, representing the Y coordinate for each vertex.
    static member inline y (value: int) = Interop.mkLightpositionAttr "y" value
    /// Numeric vector, representing the Y coordinate for each vertex.
    static member inline y (value: float) = Interop.mkLightpositionAttr "y" value
    /// Numeric vector, representing the Z coordinate for each vertex.
    static member inline z (value: int) = Interop.mkLightpositionAttr "z" value
    /// Numeric vector, representing the Z coordinate for each vertex.
    static member inline z (value: float) = Interop.mkLightpositionAttr "z" value

